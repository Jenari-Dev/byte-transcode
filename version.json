{
  "server": "3.18",
  "node": "2.12",
  "released": "2026-07-04",
  "notes": "Node fix: Whisper subtitle jobs now install cuBLAS/cuDNN and register their DLL dirs (fixes 'cublas64_12.dll not found'), with automatic CPU fallback if CUDA still can't load. GUI now logs startup errors to byte_node_gui_error.log and shows a dialog instead of silently failing.",
  "update_docs": "https://github.com/Jenari-Dev/byte-transcode#updating"
}
